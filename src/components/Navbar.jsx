import { AppBar, IconButton, Toolbar } from '@mui/material'
import React from 'react'

const Navbar = () => {
  return (
    
    <AppBar position='static' sx={{ bgcolor: "#384959" }}>
        <Toolbar>

            <IconButton>
                
            </IconButton>

        </Toolbar>
    </AppBar>

  )
}

export default Navbar